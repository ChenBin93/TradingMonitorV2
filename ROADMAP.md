# TradingMonitor V2 — 改进路线图

> 最后更新: 2026-05-12

---

## 已完成 ✓

| 事项 | 说明 |
|------|------|
| 核心骨架 | main.py / okx.py / indicators.py / signals.py / notify.py / history.py / position.py |
| 13 种信号 | BB压缩 / MA汇聚 / TTM压缩 / 多重压缩 / RSI极值 / 价格极值 / 均线排列 / ADX突破 / MACD交叉 / 放量信号 / RSI背离 / MACD背离 / 波动爆发 |
| 多 TF 确认 | 同一信号在 2+ TF 出现 → 置信度 ×1.10（2TF）或 ×1.20（3TF） |
| Stage2 入场 | 趋势突破（ADX+ROC+量） / 震荡回归（ADX<20+RSI极值），给出入场/止损/止盈/RR |
| 持仓监控 | 浮盈/浮亏告警 + 仓位变化检测 + 4 阶段自动止损 |
| 合并推送 | 预警+排名+Stage2 合并为一条消息，手机 5 秒扫完 |
| 结构化预警 | 每条信号附 4h 背景 + 1h S/R + SL/TP/RR + 清单逐项 |
| 多空统计 | 推送头显示 `多3/空1`，一秒感知市场情绪 |
| RR 过滤 | RR < 1.5 的非 critical 信号不推 |
| TOP5 排序 | 多维度评分（信号 35%+动量 25%+压缩 20%+量 20%），使用完整多 TF 数据 |
| top_n 优化 | 100→30，支持白名单模式 |
| 启动脚本 | start.sh 一行启动 |

---

## P0 — 信号有效性验证

### backtest.py：历史数据回测
- 基于 history_candles 逐根 K 线回放信号检测
- 统计每个信号的命中率、平均收益、盈亏比
- 按策略类型分别定义"有效"标准
- 按市场状态分段回测（牛市 / 熊市 / 震荡）
- 按方向分别统计（做多 / 做空命中率可能不同）

### 信号有效性定义（按策略分类）

| 策略类型 | 信号 | "有效"定义 | "命中"标准 |
|---------|------|----------|-----------|
| 波动预警 | BB/MA/TTM/多重压缩、ATR爆发 | 触发后 N 根 K 线内波幅 > X% 或 ATR 倍数 | 3 种窗口各测 |
| 方向预测 | RSI极值、均线排列、MACD交叉、背离 | 方向正确 + 反向不超 0.5% | 方向准确率 + 盈亏比 |
| 状态判断 | ADX突破、价格极值 | 状态持续（ADX 继续升 / 价格继续偏离） | 持续率 |
| 入场信号 | Stage2 趋势突破/震荡回归 | 止盈先于止损触发 | 胜率 + 平均 RR |

### 待定问题
- 时间窗口：1h / 4h / 24h 各测一份？
- 多 TF 确认组合（如 15m+1h BB 压缩）是否单独作为新信号纳入回测？

---

## P1 — 策略分类推送

### 飞书报告按策略分组

将当前 13 个信号归入三组：

**突破类**：BB压缩 / MA汇聚 / TTM压缩 / 多重压缩 / 放量信号 / ADX突破 / ATR爆发 / Stage2趋势突破
**均值回归类**：RSI极值 / 价格极值 / RSI背离 / MACD背离 / Stage2震荡回归
**趋势跟踪类**：均线排列 / MACD交叉

- 每组只推前 3 个最高置信度
- Stage2 入场信号独立展示（含入场价/止损/止盈/RR）

### 按策略区分入场规则

不所有信号同一套入场逻辑：

| 策略类型 | 入场规则 | 理由 |
|---------|---------|------|
| 突破类 | 信号触发 → 市价入 | 突破不等人，错过比亏钱更伤 |
| 趋势跟踪 | 信号触发 → 等小级别回调入 | 不回调用更小仓位追 |
| 均值回归 | 信号触发 → 挂限价单，不到不入 | 没到说明市场不认同回归判断，不入是对的 |
| 波动爆发 | 信号触发 → 市价入 | 波动启动不等人 |

### 盈亏比预估
- Stage1 信号附带盈亏比估算
- 计算方式：当前价格距离最近支撑位 / 距离最近阻力位
- 推送展示：「BTC BB压缩 入场42000 SL 41700 TP 42600 RR=2:1」
- 无需预测方向，纯价格结构计算

### 出场规则：两段式出场

取代固定止盈目标：

**第一段**：价格走到止损等距时平仓 50%（锁定利润，剩余仓位零风险）
**第二段**：剩余仓位用追踪止损跟随，让市场决定利润大小

---

## P2 — 推送门槛优化

### 提高准入门槛
- min_confidence: 0.65 → 0.78
- 只推 critical + high severity，medium/low 不推
- dedup 窗口: 30 → 60 分钟

### 缩减监控品类
- top_n: 100 → 25-30
- 取 top 20 流动性 + 用户自定义 watchlist
- config 支持白名单：
  ```yaml
  watchlist:
    - BTC/USDT:USDT
    - ETH/USDT:USDT
    - SOL/USDT:USDT
  ```

---

## P3 — 信号降噪

### 小周期过滤
- 15m 单 TF 的 RSI 极值 / MACD 交叉不单独推送
- 需 1h 同方向确认后才推
- compression_combo 保留并给最高权重
- 单独的 BB/MA/TTM 压缩提高推送门槛

### 三周期方向一致性检查

15m 做多信号需要：

```
4h: 均线多头排列 OR ADX>25 且 +DI>-DI  → 方向确认
1h: 回调到支撑 OR 横盘蓄力              → 位置确认
15m: BB压缩突破 OR RSI超卖反弹          → 入场执行
```

三个周期方向不一致 → 信号忽略。

4h 震荡时的处理：

| 价格位置 | 该做的 | 不该做的 |
|---------|--------|---------|
| 接近区间下沿 | 找 15m 做多信号 | 追空 |
| 接近区间上沿 | 找 15m 做空信号 | 追多 |
| 区间正中间 | 不做 | 任何方向 |

---

## P3 — K 线形态与价格行为维度

### 支撑阻力位计算
- 前高前低识别（水平 S/R）
- 密集成交区（Volume Profile）
- 关键整数位
- 与指标信号结合：突破信号在关键位触发更有意义

### K 线形态确认指标信号

系统推送信号后，用 K 线形态二次确认：

| 系统信号 | 对应的 K 线确认 |
|---------|--------------|
| BB 压缩 | 实体缩小 + 影线缩短（确认压缩真实） |
| RSI 超卖 | 下影线 + 拒绝（确认买方进场） |
| 均线排列 | 突破 K 线有实体 + 放量（确认方向有力） |
| 背离 | 两根连续的触及区域（确认结构有效） |

### 学习资源

**书籍**：
- 《期货市场技术分析》Murphy — 趋势/支撑阻力/反转形态三章
- 《股票大作手回忆录》Livermore — 心态与等
- 《阿尔·布鲁克斯价格行为交易》Brooks 卷一 — 信号 K 线 / 入场 K 线 / 趋势 vs 回调

**视频**（YouTube）：
- Rayner Teo: "How to Identify Support and Resistance" / "The Only Technical Analysis Guide"
- The Trading Channel: "Multi Timeframe Analysis Tutorial"

---

## P3 — 场外数据源集成

### 时段感知 + 宏观数据联动

系统自动识别当前时段（亚洲/欧洲/美盘），结合场外数据给信号加背景标签：

| 数据 | 来源 | 作用 |
|------|------|------|
| 当前时段 | 系统本地时间 | 自动识别，给信号打时段标签 |
| DXY 美元指数 | Yahoo Finance | 欧盘 DXY↑+BTC↓=风控重定价确认 |
| SPX/NDX 期货 | Yahoo Finance | 美股开盘后 SPX 方向≈BTC 同向 |
| 经济日历 | ForexFactory API | FOMC/CPI 前 1h 自动暂停推送 |
| 恐惧贪婪指数 | alternative.me API | 极端值触发全球开关 |
| 资金费率+OI | OKX API（已有） | 费率极高+OI 极高=杠杆拥挤 |
| 全网爆仓量 | Coinglass | >90% 分位→清算级联进行中 |

### 时段差异化参数

| 时段 | 突破信号 | 回归信号 |
|------|---------|---------|
| 亚洲盘 | 降权（仅 4h 同向时执行） | 正常 |
| 欧盘 16-18 | 正常→1h 甚至 4h 利润 | 正常 |
| 交接期 20:30-21:30 | 不做 | 不做 |
| 美盘确认段 | 欧盘被确认→持仓；被否定→出场 | — |

### 集成后效果

推送附带实时背景：`时段:欧盘启动 | DXY ↑0.3% | 资金费率中性`

前三项（时段/DXY/SPX/恐惧贪婪）可一小时内加完。经济日历和爆仓量后续迭代。

---

## P4 — AI 辅助

### AI 二级信号筛选

```
V2 扫描 → 一级过滤(severity+confidence) → LLM → 值得关注 / 忽略
→ 值得关注的加急 @用户
```

决策待定：
- AI 决策权：保守版（只给建议）/ 激进版（直接判定 @）
- 发给 AI 的信息量：最基本（信号类型+TF+方向+置信度）/ 丰富版（+OHLCV+多TF状态）
- 模型选择：Claude API / GPT API / 本地模型

预计成本：$0.5-1/天（个人使用可接受）

### 自动交易记录 + AI 复盘

```
OKX/Binance API → 读取成交记录 → 匹配当时的信号快照
→ LLM 生成复盘草稿（入场理由/出场评价/改进建议）
→ 用户审阅确认
```

不用用户手动记录任何一笔交易。

---

## P5 — 多品类多策略组合（远期）

### 品类扩展
- A 股: `astock.py`（新浪/东方财富 API）
- 商品期货: `commodity.py`
- 美股: `usstock.py`
- 各自独立 config + 信号阈值

### 多策略组合
- 每个策略一个配置文件，定义品类 + 信号 + 权重
- 按策略调度扫描
- 飞书报告按策略维度汇总
- 突破 / 回归 / 趋势跟踪三类策略独立运行

---

## 用户笔记

### 交易风格
- 日内短线，小资金
- 持仓周期：多数小时级别，4h 以内为主，最大 1-2 天
- 监控重心：15m 看入场，1h/4h 看方向确认
- 突破和回归策略独立运行，在不同品种/时段找符合要求的市场状态

### 交易环境
- 上班族，白天碎片时间手机看盘
- 计划：系统监控 → 飞书推送 → 手机快速判断 → 执行
- 晚上/周末复盘 + 训练 + 调代码

### 交易理念
- 以损定仓，止损不超过本金 5%（正式资金量级不超 1%）
- 认同盈亏比比预测方向更实用
- 认同信号组合 > 单独信号
- 监控核心价值：过滤噪音 > 发现更多机会
- 暂时手动执行，待系统验证充分后考虑自动化
